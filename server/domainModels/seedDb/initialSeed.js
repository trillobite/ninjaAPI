// seedDB = require('./index');

// Company.find({}).exec(function (err, collection) {
//     if (collection.length === 0) {
// 		seedDb.createDefaultCompany()
// 			.then(function(companyId){
				
// 			})

//     }
// }