var chai = require('chai');
var expect = chai.expect;
var sm = require('../server/framework/subscriptionManager');


chai.should();

describe('subscriptionManager oject', function(){
    var DataSource;
    beforeEach(function(){
        DataSource = {
            
        };
    })
    describe('object initilzation', function () {
        it('should load the proper state on intialization',function(){
            
        });
    });
    
});
